Delete in a Singly Linked List


Examples:

Input: x = 3,
       
Output: 1 -> 3
Explanation: After deleting the node at the 3rd position (1-base indexing), the linked list is as

sol/

class Solution {
    Node deleteNode(Node head, int x) {
        if (head == null) {
            return null;
        }

        if (x == 1) {
            return head.next;
        }

        Node temp = head;

        for (int i = 1; temp != null && i < x - 1; i++) {
            temp = temp.next;
        }

        if (temp != null && temp.next != null) {
            temp.next = temp.next.next;
        }

        return head;
    }
}
